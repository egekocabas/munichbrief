MunichBrief is available through:
{{- if .Values.ingress.lan.enabled }}
  https://{{ .Values.ingress.lan.host }}
{{- else }}
  kubectl -n {{ .Release.Namespace }} port-forward service/{{ include "munichbrief.fullname" . }} 8080:{{ .Values.service.webPort }}
{{- end }}
{{- if .Values.ingress.public.enabled }}
Public reader: https://{{ .Values.ingress.public.host }}
{{- end }}

SQLite uses one replica, Recreate rollout strategy, and a retained PersistentVolumeClaim.
Create an external backup before upgrades that change storage or migrations.
